{% extends '::layout/layoutAdmin.html.twig' %}

{% block body -%}
    <h1>Liste des pages supprimées</h1>

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th width="80%">Titre</th>
                <th width="20%">Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for page in pages %}
            <tr>
                <td>{{ page.titre }}</td>
                <td>
                    <a href="{{ path('adminPages_restore', { 'id': page.id }) }}"><i class="fa fa-edit"></i></a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}

<div class="col-sm-12">
    <div class="well">
        <ul class="nav nav-list">
            <li class="nav-header">Panier</li>
            <li>
                <a href="{{ path('panier') }}">
                    {% if articles == 0 %}
                        Aucun article dans le panier
                    {% elseif articles == 1 %}
                        1 article dans le panier
                    {% else %}
                        {{ articles }} articles dans le panier
                    {% endif %}
                </a>
            </li>
        </ul>
    </div>
</div>
